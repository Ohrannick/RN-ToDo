import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
  root: {
    height: '100%',
    backgroundColor: 'white',
    padding: 25
  },
  todoText: {
    marginVertical: 10,
    fontSize: 17
  }
})