import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
  container: {
    paddingBottom: 100
  },
  root: {
    height: '100%',
    backgroundColor: 'white',
    padding: 25,
    margin: 15,
  },
})