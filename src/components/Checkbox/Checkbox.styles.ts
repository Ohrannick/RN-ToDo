import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  box: {
    height: 20,
    width: 20,
    borderRadius: 10,
    borderWidth: 1,
    borderColor: 'black',
  },
  filled: {
    backgroundColor: 'darkgreen',
  },
});